# Ẩn thư mục node_modules để tránh đẩy lên Git
node_modules/
node_modules/*

# Ẩn các file build và dependency
dist/
build/
.cache/

# Ẩn file môi trường (chứa thông tin nhạy cảm như API keys)
#.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Ẩn thư mục chứa log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
debug.log*

# Ẩn các file hệ thống và editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ẩn output của ESLint / Prettier nếu có
eslintcache
.prettier-cache/

# Ẩn coverage report nếu bạn chạy test với Jest
coverage/

# Ẩn file lock nếu bạn dùng npm, yarn hoặc pnpm (chỉ giữ lại cái bạn cần)
package-lock.json
yarn.lock
pnpm-lock.yaml

# Ẩn thư mục dành cho GitHub Actions (nếu có)
.github/

# Ẩn service worker cache (nếu bạn dùng PWA)
workbox-config.js
